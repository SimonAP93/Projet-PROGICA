{% extends "base.html.twig" %}

{% block body %}
    <div class="row">
    <div class="col-md-8 m-5">
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <input type="submit" value="Save" class="btn btn-primary m-2">
    {{ form_end(form) }}
    </div>
    </div>
{% endblock %}